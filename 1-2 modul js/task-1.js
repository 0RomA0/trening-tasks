// Задача 1
// Даний рядок, що складається із символів, наприклад, 'hello'.
// Перевірте, що останній символ цього рядка — літера 'o'.
// Якщо це так — виведіть 'так', інакше виведіть 'ні'.


// 1)
const str = 'hello';
if (str.endsWith('o')) {
    console.log('так');
} else {
    console.log('ні');
}


// 2)
// console.log(str[str.length -1] === 'o' ? 'так' : 'ні');